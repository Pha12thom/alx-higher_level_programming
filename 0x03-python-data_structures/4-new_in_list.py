#!/usr/bin/python3
if __name__ == "__main__":
    def new_in_list(my_list, idx, element):
        if idx < 0 or num > len(my_list):
            return my_list
        my_list[idx] = element
        return my_list
